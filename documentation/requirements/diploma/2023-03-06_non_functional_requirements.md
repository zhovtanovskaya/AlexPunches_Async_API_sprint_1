# Не функциональные требования к "Кино вместе"

```
    Начинать фильм у всех пользователей одновременно
        GetCurrentState
            WwbSocket API или REST API?
            Отвечать объектом CurrentState
        Посылать команды плеера от организатора на сервер
            Отвечать ACK на команду плеера
        И от сервера перенаправлять команды плеера гостям
        Использовать на Python либу websockets?
    Доставка сообщений
        Гарантия доставки "More than once"
        Порядок добавления сообщений соблюден
        Подтверждение от сервера плееру, что сообщение принято
            WebSocket из коробки поддерживает?
    Backoff подключения к БД
    Исследование БД
        Redis Cluster
        Mongo
        Dynamo DB
        Clasterade?
        Должна поддерживать много консьюмеров, и мало продюсеров
        Горизонтальное масштабирование
    Встроить Video Player на HTML-страницу
        Plyr
        HTML5 
        С видеороликом
        Плеер и чат должен перепосылать событие, пока не получит ACK от сервера на него
    Авторизация пользователя при подключении к комнате
```